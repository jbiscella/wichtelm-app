<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>net.jacopobiscella</groupId>
  <artifactId>wichtelm-app</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>wichtelm-app</name>
  <description>End-user backtesting application for trading strategies on single instruments.</description>
  <inceptionYear>2026</inceptionYear>

  <organization>
    <name>Jacopo Biscella</name>
  </organization>

  <licenses>
    <license>
      <name>0BSD</name>
      <url>https://opensource.org/license/0bsd</url>
      <distribution>repo</distribution>
      <comments>BSD Zero Clause License</comments>
    </license>
  </licenses>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>25</maven.compiler.release>

    <hatrack.version>0.48.0-alpha</hatrack.version>
    <tomlj.version>1.1.1</tomlj.version>
    <cucumber.version>7.34.3</cucumber.version>
    <junit.bom.version>5.14.4</junit.bom.version>

    <compiler.plugin.version>3.15.0</compiler.plugin.version>
    <surefire.plugin.version>3.5.5</surefire.plugin.version>
    <shade.plugin.version>3.6.2</shade.plugin.version>
    <exec.plugin.version>3.5.0</exec.plugin.version>
    <antrun.plugin.version>3.2.0</antrun.plugin.version>
    <license.plugin.version>2.5.0</license.plugin.version>
    <dependency.plugin.version>3.8.1</dependency.plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.bom.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- ha-track libraries (Maven Central) -->
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>commons</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>indicators</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>frau-holle</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>frau-holle-csv</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>frau-holle-eodhd</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>nachtkrapp</artifactId>
      <version>${hatrack.version}</version>
    </dependency>
    <dependency>
      <groupId>net.jacopobiscella</groupId>
      <artifactId>heerwisch-jfreechart</artifactId>
      <version>${hatrack.version}</version>
    </dependency>

    <!-- TOML config parsing -->
    <dependency>
      <groupId>org.tomlj</groupId>
      <artifactId>tomlj</artifactId>
      <version>${tomlj.version}</version>
    </dependency>

    <!-- Test: Cucumber-JVM on the JUnit Platform -->
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-java</artifactId>
      <version>${cucumber.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.cucumber</groupId>
      <artifactId>cucumber-junit-platform-engine</artifactId>
      <version>${cucumber.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-suite</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>wichtelm</finalName>
    <plugins>
      <!-- Auto-generates the project LICENSE file and the THIRD-PARTY.txt
           dependency-license report during the generate-resources phase, so
           both are refreshed on every build. The 0BSD license is supplied via
           the custom license repository under src/license. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>license-maven-plugin</artifactId>
        <version>${license.plugin.version}</version>
        <configuration>
          <licenseName>bsd_0</licenseName>
          <licenseResolver>${project.baseUri}src/license</licenseResolver>
          <licenseFile>${project.basedir}/LICENSE</licenseFile>
          <inceptionYear>2026</inceptionYear>
          <organizationName>Jacopo Biscella</organizationName>
        </configuration>
        <executions>
          <execution>
            <id>generate-project-license</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>update-project-license</goal>
            </goals>
          </execution>
          <execution>
            <id>generate-third-party-licenses</id>
            <phase>generate-resources</phase>
            <goals>
              <goal>add-third-party</goal>
            </goals>
            <configuration>
              <!-- THIRD-PARTY.txt is written to the repository root and
                   reflects only the dependencies shipped in the JAR. -->
              <outputDirectory>${project.basedir}</outputDirectory>
              <thirdPartyFilename>THIRD-PARTY.txt</thirdPartyFilename>
              <excludedScopes>test</excludedScopes>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${compiler.plugin.version}</version>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>${surefire.plugin.version}</version>
        <configuration>
          <systemPropertyVariables>
            <java.awt.headless>true</java.awt.headless>
          </systemPropertyVariables>
        </configuration>
      </plugin>

      <!-- JFreeChart is LGPL-2.1: it must NOT be merged into the shaded
           uber-JAR (LGPL section 6 requires the end user be able to relink
           a modified copy). It is instead copied next to the JAR as a
           separate, user-replaceable artifact and referenced via the
           manifest Class-Path. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>${dependency.plugin.version}</version>
        <executions>
          <execution>
            <id>copy-lgpl-jfreechart</id>
            <phase>prepare-package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <includeArtifactIds>jfreechart</includeArtifactIds>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
              <stripVersion>true</stripVersion>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${shade.plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <!-- LGPL JFreeChart stays out of the uber-JAR; it is loaded
                   at runtime from the sibling lib/jfreechart.jar. -->
              <artifactSet>
                <excludes>
                  <exclude>org.jfree:jfreechart</exclude>
                </excludes>
              </artifactSet>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>net.jacopobiscella.wichtelm.cli.WichtelmCli</mainClass>
                  <manifestEntries>
                    <Class-Path>lib/jfreechart.jar</Class-Path>
                    <Implementation-Version>${project.version}</Implementation-Version>
                  </manifestEntries>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Produces the native `wichtelm` CLI launcher (jpackage app-image)
           alongside the executable JAR, per CLAUDE.md section 1. Runs after
           the shade plugin in the package phase. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>${antrun.plugin.version}</version>
        <executions>
          <execution>
            <id>jpackage-app-image</id>
            <phase>package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <delete dir="${project.build.directory}/jpackage-input"/>
                <delete dir="${project.build.directory}/dist"/>
                <mkdir dir="${project.build.directory}/jpackage-input"/>
                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                      todir="${project.build.directory}/jpackage-input"/>
                <!-- Ship the LGPL JFreeChart JAR alongside the uber-JAR so
                     the manifest Class-Path resolves and the library stays
                     separately replaceable. -->
                <copy todir="${project.build.directory}/jpackage-input/lib">
                  <fileset dir="${project.build.directory}/lib"/>
                </copy>
                <exec executable="${java.home}/bin/jpackage" failonerror="true">
                  <arg value="--type"/>
                  <arg value="app-image"/>
                  <arg value="--name"/>
                  <arg value="wichtelm"/>
                  <arg value="--input"/>
                  <arg value="${project.build.directory}/jpackage-input"/>
                  <arg value="--main-jar"/>
                  <arg value="${project.build.finalName}.jar"/>
                  <arg value="--main-class"/>
                  <arg value="net.jacopobiscella.wichtelm.cli.WichtelmCli"/>
                  <arg value="--dest"/>
                  <arg value="${project.build.directory}/dist"/>
                </exec>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Runs the demo data tools (e.g. DemoDataDownloader) against the
           compiled classpath. Invoke with -Dexec.mainClass and -Dexec.args. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>${exec.plugin.version}</version>
      </plugin>
    </plugins>
  </build>

</project>
