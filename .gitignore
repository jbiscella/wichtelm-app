# Claude Code local settings (per-developer, not shared)
.claude/settings.local.json

# Maven build output
target/
dependency-reduced-pom.xml

# IDE
.idea/
*.iml
.vscode/

# scratch / local data dumps (not source)
tmp1
tmp2
skills/wichtelm-strategy-author.zip
